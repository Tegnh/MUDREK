NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
GEMINI_API_KEY=

# --- نظام المتابعة (التذكيرات المجدولة) ---------------------------------
# كلها اختيارية: بدونها يعمل المحرّك في وضع الجفاف — يقرّر ويعرض النتيجة في
# /admin/cron دون أن يغادر بريد.

# مفتاح Resend. بدونه لا يُرسل شيء ولا يفشل شيء.
RESEND_API_KEY=

# المُرسِل. الافتراضي onboarding@resend.dev ويصل إلى بريد صاحب حساب Resend
# وحده — كافٍ للعرض، ويحتاج نطاقًا موثَّقًا للإنتاج.
RESEND_FROM=

# أساس الروابط داخل الرسائل. الافتراضي http://localhost:3000
NEXT_PUBLIC_SITE_URL=

# حارس /api/cron/reminders. يُضبط في Vercel فيُرسله Cron في ترويسة
# Authorization. على الإنتاج بدونه يُغلق المسار بدل أن يعمل مكشوفًا.
CRON_SECRET=
