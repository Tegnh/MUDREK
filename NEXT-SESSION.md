# برومنت الجلسة القادمة

انسخ ما بين الخطين والصقه في دردشة Claude Code جديدة داخل `c:\project\مدرك`.

---

أعمل على **مُدرِك** — منصة تشخيص وتعلّم بالذكاء الاصطناعي، عربية RTL، بنافذتين: المعلم (`/t`) والطالب (`/s`).
التقنيات: Next.js 15.5 (App Router, Server Actions) · React 19 · Tailwind v4 · Supabase (Auth + Postgres + RLS) · Gemini عبر `@google/genai`.

اقرأ `readme.md` و`lib/data/types.ts` و`lib/data/store.ts` أوّلًا قبل أي تعديل.

## الحالة الحالية — اقرأها كي لا تعيد اكتشافها

**المصادقة تعمل بالكامل**: بريد + كلمة مرور (`lib/auth/actions.ts`)، وصفحتا `/login` و`/signup`، ورابط الدخول (Magic Link) خيار ثانوي. إرسال البريد **معطّل في هذه البيئة**، لذلك إنشاء الحساب يمرّ بـ `admin.createUser({ email_confirm: true })` ثم يسجّل الدخول فورًا. `lib/auth/session.ts` فيه `requireRole` و`getProfile` (مغلّفة بـ `cache()`) و`ensureProfile`. لا تلمس هذا المسار إلا لسبب.

**نافذة المعلم حقيقية** — تقرأ وتكتب في Supabase (فصول، رمز انضمام، اختبارات، تشخيصات).

**نافذة الطالب وهمية** — وهذه أهم نقطة: كل مسار الطالب (الملفات، الأقسام، الاختبارات، المحاولات، السلسلة، الفجوات) يعمل على **مخزن في الذاكرة** في `lib/data/store.ts` مثبَّت على `globalThis`. أي أنه:
- مشترك بين **كل** الحسابات — لا عزل لكل مستخدم،
- يضيع عند إعادة تشغيل الخادم،
- لا علاقة له بجداول `sources` / `sections` / `quizzes` / `attempts` الموجودة فعلًا في `supabase/migrations/001_init.sql` مع سياسات RLS جاهزة.

**Gemini يعمل**: المفتاح في `.env`، والنموذج `gemini-3.5-flash` في `lib/ai/client.ts`. **لا ترجع إلى `gemini-2.5-*`** — تُرجِع 404 لهذا المفتاح (`no longer available to new users`). `BASE_CONFIG` يضبط `thinkingLevel: LOW` للسرعة.

**هجرة `003_password_auth_and_role_claim.sql`** قد لا تكون طُبِّقت بعد على Supabase — تحقّق قبل الاعتماد على `app_metadata.role`.

## المطلوب

### ٠) قرار البداية: أين تُخزَّن هذه الميزات؟
الميزات الثلاث أدناه كلها **حالة لكل مستخدم تتراكم عبر الأيام** (جدولة مراجعة، بطاقات، أخطاء سابقة). المخزن الحالي في الذاكرة يفقدها عند كل إعادة تشغيل ويخلط بين الحسابات — فبناؤها فوقه يعني بناء شيء لا يعمل فعليًا.

ابدأ بعرض خيارين عليّ مع تكلفة كل منهما قبل كتابة أي كود:
- (أ) ترحيل مسار الطالب إلى Supabase أوّلًا (الجداول والسياسات موجودة أصلًا)، ثم بناء الميزات فوقه،
- (ب) البقاء على المخزن الوهمي مؤقتًا مع عزله بـ `student_id` على الأقل.

لا تختر عني.

### ١) إعدادات الاختبار
عند رفع مصدر دراسي، أريد اختيار:
- **لغة الاختبار**: عربي أو إنجليزي،
- **عدد الأسئلة لكل قسم**: 4 أو 6 أو 10.

الوضع الحالي: `lib/data/ingest.ts` ينادي `generateQuiz(rs.content_md, 4, catalog)` — العدد مثبَّت على 4، ولا معامل لغة إطلاقًا. `generateQuiz` في `lib/ai/generateQuiz.ts` يقبل `count` بين 1 و50 بالفعل، فالناقص هو تمرير الخيار من `app/s/upload/UploadForm.tsx` عبر `app/s/upload/actions.ts` إلى `ingest.ts`، وإضافة اللغة إلى البرومبت والمخطط. احفظ الخيارين مع الملف نفسه ليُعاد استخدامهما عند توليد اختبار لاحق لأي قسم.

### ٢) الفجوات + نظام مراجعة على طريقة Duolingo
أريد كل سؤال **أجبته خطأً** أن يظهر في `/s/gaps` بشكل نظيف ومرتّب، مع نظام تكرار متباعد يعيد عليّ الأسئلة الخاطئة حتى أتقنها.

الوضع الحالي: `app/s/gaps/page.tsx` يعرض `getGaps()` وهي مبنية على **تشخيصات المعلم** (`SEED_DIAGNOSES`) لا على أخطائي أنا. لكن `recordAnswer` في `store.ts` يسجّل بالفعل كل محاولة في نوع `Attempt` (فيه `isCorrect` و`misconceptionId` و`questionIndex` و`sectionId` و`createdAt`) — فالبيانات موجودة وغير معروضة.

المطلوب:
- قائمة أخطائي مجمَّعة بالمفهوم الخاطئ (`misconception_id`) لا كسرد أسئلة متفرّقة،
- جدولة مراجعة (فترات متباعدة: نفس اليوم ← ٢ ← ٤ ← ٧ ← ١٥ يومًا مثلًا)، مع تراجع البند إلى بداية السلسلة عند الخطأ مجددًا،
- شارة «مستحقّ اليوم» وعدد البنود المستحقّة،
- إخراج البند من الدورة بعد إتقانه (عرّف الإتقان بوضوح واذكره لي).

اربطه بـ `StreakChip` الموجود إن كان منطقيًا. راجع `MASTERY_THRESHOLD` في `lib/data/types.ts` قبل أن تخترع عتبة جديدة.

### ٣) نظام بطاقات تعليمية (Flashcards)
بطاقات مولَّدة من المصادر المرفوعة (وجه/ظهر)، بلغة الاختبار المختارة، مع تراجع محلي في `lib/ai/fallback.ts` كما هو الحال في بقية دوال الذكاء الاصطناعي. اجعلها تشترك في محرّك الجدولة نفسه في البند (٢) بدل محرّك ثانٍ منفصل.

### ٤) إصلاح عرض المحتوى (خلل ظاهر الآن)
`components/student/Markdown.tsx` عارض مصغّر يدعم `##` و`###` والقوائم و`**عريض**` فقط. النتيجة في صفحة الدرس:
- `*مائل*` بنجمة واحدة يظهر بنجومه حرفيًا،
- `####` لا يُعالَج،
- معادلات LaTeX تظهر خامًا: `$\nabla J(\theta_t) = \frac{\partial J}{\partial \theta}$`،
- النصّ الإنجليزي داخل حاوية RTL تتطاير علامات ترقيمه إلى بداية السطر.

أصلحها: وسّع العارض (أو استبدله بمكتبة إن رأيتها أفضل — قارن وأوصِ)، وأضف `dir="auto"` على مستوى الفقرة ليصحّ خلط العربي بالإنجليزي، وقرّر كيف نتعامل مع LaTeX (عرضه بخط أحادي المسافة على الأقل، أو KaTeX).

## قواعد العمل

- **العربية RTL هي الأساس** في كل نصّ ظاهر وكل تعليق في الكود — التزم بأسلوب التعليقات الموجود (تشرح *لماذا* لا *ماذا*).
- **لا تكسر نظام التصميم**: استخدم مكوّنات `components/ui/` القائمة (`Button`, `Card`, `Input`, `Badge`, `EmptyState`…) وصفحة `/design` مرجعها. البرتقالي (`--color-accent`) محجوز للفجوة/النقص فقط — لا تستخدمه كلون إجراء.
- شغّل `npm run typecheck` و`npx next build` قبل أن تعلن الانتهاء.
- لا تنشئ حسابات تجريبية في Supabase دون حذفها بعد الاختبار.
- ابدأ بخطة قصيرة للبند (٠) واعرضها عليّ قبل التنفيذ.
